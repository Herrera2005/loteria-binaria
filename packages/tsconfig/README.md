# @loteria-binaria/tsconfig

Configuraciones TypeScript compartidas.

- `base.json`: reglas estrictas comunes.
- `library.json`: compilación de paquetes con declaraciones.
- `node.json`: configuración base para procesos Node.js.

Este paquete no contiene lógica de dominio.
